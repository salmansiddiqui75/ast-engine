package com.ats.engine.respository;

import com.ats.engine.model.Rule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RuleRepository1 extends JpaRepository<Rule, Long> {
}
